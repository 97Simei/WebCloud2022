apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user
spec:
  ports:
    - port: 5001
      nodePort: 30007
      targetPort: 5001
  selector:
    app: user
  type: NodePort
